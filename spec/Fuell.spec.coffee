describe "Fuell", ->
  # _ = require "../src/Fuell"
        
  # describe "matches", ->
  #   it "works", ->
  #     expect(_.mapped [[_.matches, _.isEven], [_.mapped, [_.sum, 2]]], [[0, 1, 2, 3], [8]])
  #       .toEqual [[2, 4], [10]]

  # describe "firstMatch", ->
  #   it "works", ->
  #     expect(_.firstMatch _.isEven, [1,1,3,8,4,5,6]).toEqual 8

  # describe "mapped", ->
  #   it "must work", ->
  #     expect(_.mapped [_.sum, 2], [0, 1, 2, 3]).toEqual [2, 3, 4, 5]
  
  # 